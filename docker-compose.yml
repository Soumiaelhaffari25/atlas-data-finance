version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: atlas_finance_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: atlas_user
      POSTGRES_PASSWORD: atlas_password
      POSTGRES_DB: atlas_finance
    ports:
      - "5432:5432"
    volumes:
      - atlas_pgdata:/var/lib/postgresql/data

volumes:
  atlas_pgdata:
